#summary Liste des futurs implémentations et corrections

= BETA 1.2 =

  # Correction et Amélioration des exemples dans iViewer 
  # Afficher la dernière action (et trier par date) les équipements trouvés dans "Liste des équipements non trouvés"
  # Ajout de la gestion des status pour les scenarios temporels (ACTION_IN_TIME et ACTION_FOR_TIME)
  # Ajout du support des paramètres pour les commandes Lexic (inter, poussoir, tempo, etc.), pour détecter le bon status
  # Ajout des commandes d'arrêt pour interrompre les scripts en cours (stop_command, stop_favoris, stop_macros et stop_trame)
  # Ajout de délais pour les commandes en cours (delay_command, delay_favoris, delay_macros et delay_trame)
  # Ajout des déclencheurs

= BETA 1.1 =

  # ~~Corriger la référence 84541 en attente de validation par Frederic~~ -> MYSQL Table references
  # ~~Ajout de la référence 67255 en attente de validation par Aurélien~~ -> MYSQL Table references
  # ~~Ajout de la programmation des scénarios des équipements sans les ouvrir et sans utiliser la procédure classique de Legrand, plus besoin de retirer les enjoliveurs :)~~ -> équipements.js
  # ~~Bug dans "Liste des scénarios de l'équipement" certains Id/Unit Legrand n'était pas affichés~~ -> equipements.js
  # ~~Bug dans "ajout d'un équipement" si la liste des références n'a pas été dépliée les références sont inconnues~~ -> equipements.js
  # ~~Bug dans "ajout d'un équipement" la liste des références n'affiche que les références listées dans "liste des références supportées par le serveur"~~ -> equipements.js
  # ~~Bug dans la génération des trames lorsque le unit est supérieur à 9 ~~ -> own_scripts.js
  # ~~Bug lorsque le serveur ne contient aucun jalon, fichier de log boxio.err qui gonfle, merci Sylvain~~ -> server.php

= BETA 1.0 =

  # ~~Problèmes d'accents dans "Ajout d'un équipement" lors de l'enregistrement (Bug UTF-8)~~ -> client.php
  # ~~Automatiser le rafraichissement dans "Liste des équipements non trouvés"~~ -> equipements.js
  # Afficher la dernière action (et trier par date) les équipements trouvés dans "Liste des équipements non trouvés"
  # ~~Bug lors de la mise à jour d'un équipement pour changer la zone, 2 zones sont crées dans "Modification d'un équipement"~~ -> MYSQL Routine add_equipement
  # ~~Vérifier le retour lors de la programmation automatique d'un scenario et indique une erreur si nécessaire dans "liste des scenarios de l'équipement"~~ -> équipements.js
  # ~~Décrypter les paramètres dans "Etat du Bus" pour faciliter la compréhension~~ -> trame.js
  # ~~Bug dans "Modification d'un jalon" la mise à jour créée un nouveau jalon~~ -> cron.js
  # ~~Bug lorsque le serveur ne contient aucun jalon, blocage complet du BUS, merci Patrice pour les corrections :)~~ -> crond.php, server.php
  # ~~Ajouter les références 67201 et 67202 demande de frederic~~ -> MYSQL Table references
  # ~~Les jalons ne sont pas mises à jour tant que le serveur n'a pas redémaré~~ -> crond.php, server.php