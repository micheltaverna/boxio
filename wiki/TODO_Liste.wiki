#summary Liste des futurs implémentations et corrections

= TODO BETA 1.0 =

  # ~~Problèmes d'accents dans "Ajout d'un équipement" lors de l'enregistrement (Bug UTF-8)~~ -> client.php
  # ~~Automatiser le rafraichissement dans "Liste des équipements non trouvés"~~ -> equipements.js
  # Afficher la dernière action (et trier par date) les équipements trouvés dans "Liste des équipements non trouvés"
  # ~~Bug lors de la mise à jour d'un équipement pour changer la zone, 2 zones sont crées dans "Modification d'un équipement"~~ -> MYSQL Routine add_equipement
  # ~~Vérifier le retour lors de la programmation automatique d'un scenario et indique une erreur si nécessaire dans "liste des scenarios de l'équipement"~~ -> équipements.js
  # ~~Décrypter les paramètres dans "Etat du Bus" pour faciliter la compréhension~~ -> trame.js
  # ~~Bug dans "Modification d'un jalon" la mise à jour créée un nouveau jalon~~ -> cron.js
  # ~~Bug lorsque le serveur ne contient aucun jalon, blocage complet du BUS, merci Patrice pour les corrections :)~~ -> crond.php, server.php
  # ~~Ajouter les références 67201 et 67202 demande de frederic~~ -> MYSQL Table references
  # Corriger la référence 84541 en attente de validation par frederic
  # ~~Les jalons ne sont pas mises à jour tant que le serveur n'a pas redémaré~~ -> crond.php, server.php